import 'package:flutter/material.dart';
import 'package:tip_calculator/features/components/Body.dart';

class LoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}